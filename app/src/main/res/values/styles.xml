<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/app_theme</item>
        <item name="colorPrimaryDark">@color/app_statusbar</item>
        <item name="colorAccent">@color/app_theme</item>
        <item name="android:textColorPrimary">@color/app_text_color</item>
        <item name="android:windowContentTransitions" tools:ignore="NewApi">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.NoActionBar">


        <item name="windowNoTitle">true</item>//无标题
        <item name="windowActionBar">false</item>//无ActionBar
        <item name="android:windowFullscreen">false</item>//全屏即无通知栏
        <item name="android:windowContentOverlay">@null</item>//是否有遮盖
    </style>


    <!-- tipView 动画 -->
    <style name="style_anim_tipsView">
        <item name="android:windowEnterAnimation">@anim/tips_in</item>
        <item name="android:windowExitAnimation">@anim/tips_out</item>
    </style>

    <!--LoadingDialog Style-->
    <style name="style_loadingDialog" parent="@android:style/Theme.Dialog">
              
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
           
    </style>

</resources>
